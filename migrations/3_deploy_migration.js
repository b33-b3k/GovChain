// const BudgetAllocation = artifacts.require("BudgetAllocation");

// module.exports = function (deployer) {
//   // Deploy the BudgetAllocation contract with an initial admin address
//   deployer.deploy(BudgetAllocation)
//     .then(async () => {
//       // Access the deployed contract instance
//       const budgetAllocationInstance = await BudgetAllocation.deployed();

//       // Perform any additional setup or configuration if needed
//       // For example, you can set the initial admin address here
//       // await budgetAllocationInstance.initializeAdmin(adminAddress);

//       console.log("BudgetAllocation contract deployed at:", budgetAllocationInstance.address);
//     });
// };
